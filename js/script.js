'use strict';

function phone(brand, price, color, memory, premiere) {
    this.brand = brand;
    this.price = price;
    this.color = color;
    this.memory = memory;
    this.premiere = premiere;
}